/**
 * Core Hooks Module
 * Universal hook system for cross-provider event handling
 */

const { UniversalHook, UniversalEvents, ClaudeCodeEvents } = require('./universalHook');

module.exports = {
  UniversalHook,
  UniversalEvents,
  ClaudeCodeEvents
};
