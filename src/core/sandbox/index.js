/**
 * Sandbox & Permissions Module
 *
 * Unified permission system for multi-provider environments
 * (Claude Code, OpenCode, etc.)
 *
 * @module core/sandbox
 */

const { UnifiedPermissions, PermissionType } = require('./permissions');

module.exports = {
  UnifiedPermissions,
  PermissionType
};
